{
  "apiEndPoints": ${apiEndPoints},
  "apiInvokeUrl": ${apiInvokeUrl},
  "cognitoUserPoolId": "${cognitoUserPoolId}",
  "cognitoClientId": "${cognitoClientId}",
  "loginUrl": "${loginUrl}",
  "region": "${region}"
}
